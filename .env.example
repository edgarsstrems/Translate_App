# ==============================================================================
# Church Sermon Translator Configuration
# ==============================================================================

# ------------------------------------------------------------------------------
# 1. Gemini AI Translation (Recommended)
# ------------------------------------------------------------------------------
# Get your Gemini API key from Google AI Studio: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=

# Options:
# - gemini-2.0-flash       (Recommended - Fast & Reliable)
# - gemini-2.0-flash-lite  (High Speed, 30 RPM)
# - gemini-1.5-flash       (Classic Fast)
# - gemini-1.5-flash-8b    (Lowest Latency)
GEMINI_MODEL=gemini-2.0-flash

# Translation provider: "gemini" (recommended), "google_cloud", or "auto"
TRANSLATION_PROVIDER=gemini

# Free Tier Optimization Mode: translates EN + RU jointly in a single API call
FREE_TIER_MODE=true

# ------------------------------------------------------------------------------
# 2. Speech Recognition (STT)
# ------------------------------------------------------------------------------
# Backend: "openai" (Cloud API - fast & accurate) or "local" (Faster-Whisper on PC)
SPEECH_RECOGNITION_BACKEND=openai

# OpenAI API Key (Required if SPEECH_RECOGNITION_BACKEND=openai)
OPENAI_API_KEY=

# OpenAI STT Model: "gpt-4o-mini-transcribe" (Fast), "whisper-1" (Whisper Large V2), or "gpt-4o-transcribe"
OPENAI_TRANSCRIPTION_MODEL=gpt-4o-mini-transcribe

# Local Whisper settings (Used if SPEECH_RECOGNITION_BACKEND=local)
# Model size: "large-v3-turbo" (Recommended - Best Accuracy & Speed), "medium", "large-v3", "small"
WHISPER_MODEL_SIZE=large-v3-turbo
WHISPER_BEAM_SIZE=3
WHISPER_DEVICE=auto
WHISPER_COMPUTE_TYPE=auto
WHISPER_VAD_FILTER=false
WHISPER_HOTWORDS=
# Quality mode: "balanced", "live", "accuracy"
WHISPER_QUALITY_MODE=balanced
WHISPER_MIN_RMS=0.0

# ------------------------------------------------------------------------------
# 3. Google Cloud Translation & Text-to-Speech (Optional)
# ------------------------------------------------------------------------------
# Optional Google Cloud Translation API Key
GOOGLE_TRANSLATE_API_KEY=

# Path to Google Service Account JSON for Google Cloud Text-to-Speech
# (If not provided, the app uses built-in instant free speech synthesis)
GOOGLE_APPLICATION_CREDENTIALS=credentials\google-service-account.json

# Voice names for Google Cloud TTS
TTS_ENGLISH_VOICE=en-US-Standard-J
TTS_RUSSIAN_VOICE=ru-RU-Standard-D

# ------------------------------------------------------------------------------
# 4. Audio Capture & Latency Tuning
# ------------------------------------------------------------------------------
# Dynamic chunk duration in seconds (10s max ceiling, 1.8s dynamic min, 0.70s natural sentence pause)
CHUNK_SECONDS=10.0
MIN_CHUNK_SECONDS=1.8
EARLY_FLUSH_SILENCE_SECONDS=0.70
CHUNK_OVERLAP_SECONDS=0.0

# Smart Semantic Sentence Stitching: Buffers incomplete clauses & stitches across chunks
SMART_SENTENCE_STITCHING=true

# Voice Activity Detection (VAD) settings
VAD_ENABLED=true
VAD_RMS_THRESHOLD=0.004
VAD_PEAK_THRESHOLD=0.025
VAD_MIN_SPEECH_SECONDS=1.2
VAD_MIN_SPEECH_RATIO=0.06
VAD_PADDING_SECONDS=0.75

# Queue management
MAX_AUDIO_QUEUE_SIZE=2
MAX_TRANSLATION_QUEUE_SIZE=6
MAX_CHUNK_AGE_SECONDS=45
MAX_TTS_AGE_SECONDS=60
SAVE_DEBUG_AUDIO=false
